# Test Environment Variables
NODE_ENV=test
PORT=5001

# Database Configuration (Test DB)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=authentication_test_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_ACCESS_SECRET=test-jwt-access-secret-key
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Google OAuth (Test credentials)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5001/api/auth/google/callback

# Email Configuration (Test - no actual emails sent)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASSWORD=test-password
EMAIL_FROM=noreply@test.com

# Frontend URL
CLIENT_URL=http://localhost:3000

